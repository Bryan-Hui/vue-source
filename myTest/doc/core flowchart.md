用户创建实例
     │
     ▼
┌─────────────────────────────────────┐
│ new Vue(options)                     │
│   → _init() 初始化                   │
└─────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────┐
│ 初始化阶段                           │
│ ├─ 合并配置 (mergeOptions)          │
│ ├─ 初始化生命周期                   │
│ ├─ 初始化事件                       │
│ ├─ 初始化渲染                       │
│ ├─ beforeCreate 钩子                │
│ ├─ 初始化状态 (响应式核心) ⭐       │
│ │  ├─ props                         │
│ │  ├─ methods                       │
│ │  ├─ data (变成响应式)             │
│ │  ├─ computed                      │
│ │  └─ watch                         │
│ ├─ created 钩子                     │
│ └─ 如果有 el → $mount               │
└─────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────┐
│ 模板编译 (完整版)                    │
│ template → AST → render 函数         │
└─────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────┐
│ 挂载阶段 ($mount)                    │
│ ├─ beforeMount                      │
│ ├─ render() → VNode                │
│ ├─ update() → patch() ⭐           │
│ └─ mounted                          │
└─────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────┐
│ 响应式更新 (数据变化)                │
│ ├─ setter → Dep.notify()           │
│ ├─ Watcher.update() → 加入队列     │
│ ├─ nextTick 异步执行               │
│ ├─ beforeUpdate                    │
│ ├─ 重新 render → patch()           │
│ └─ updated                          │
└─────────────────────────────────────┘